void setup() {
/* Initlaize 
 * serial coms
 * lcd
 * motor shield
 * I2C initalize - Wire.h
 * mpu 6050 - offsets?
 * Initialize PID parameters 
 * 
 *  call a gravity vector set up fucntion
 *  call a PID set up function that uses the LCD display
 */

 
}

void loop() {
/* Define a delta t
 * Sample the IMU at every t incremnet 
 * use kalman filter on gyro and accel data
 * calcualte error
 * send erro to PID for correction value
 * Output correction PWM to motors 
 */



 
}
